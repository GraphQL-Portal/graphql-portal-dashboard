scalar Timestamp

type Gateway {
  nodeId: String
  lastPingAt: Timestamp
  configTimestamp: Timestamp
}

type Query {
  getGateways: [Gateway]
}

type Mutation {
  registerGateway(nodeId: String): Boolean
}

type Subscription {
  gatewaysUpdated: [Gateway]
}
