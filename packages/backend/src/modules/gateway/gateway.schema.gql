scalar Timestamp

type Gateway {
  nodeId: String
  lastPingAt: Timestamp
  configTimestamp: Timestamp
  hostname: String
  status: String
  listenHostname: String
  listenPort: Int
}

type Query {
  getGateways: [Gateway]
}

type Subscription {
  gatewaysUpdated: [Gateway]
}
