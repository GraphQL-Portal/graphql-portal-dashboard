scalar Any

type Chart {
  latency: [ChartChunk]
  count: [ChartChunk]
  countries: [ChartChunk]
  failures: [ChartChunk]
}

type ChartChunk {
  argument: Any
  value: Any
  success: Any
  failure: Any
}

type Query {
  metrics(startDate: Timestamp!, endDate: Timestamp!, scale: String): Chart
}
